# T6 — Observability — 2026-07-01 audit appendix

Scope: pipeline_events coverage vs real transitions, the two enforce decisions (EVENT_AWARE_RECHECK, PRE_FILTER_ENFORCE_BUCKETS), and operator-deception surfaces. All citations verified at HEAD `19ff594` in `/Users/smokke/Documents/discord/.claude/worktrees/audit-2026-07`. Live numbers are from the probe pack, not re-queried.

### T6-01 [P1] [confidence: high] The two silent terminal-grade writers — 7-day sweep LOSS and celebration auto-grade — emit ZERO telemetry (no pipeline_events, no grading_audit), so every #161 health surface shows healthy while they coin grades
- Where: services/grading.js:1908 (sweep → `gradeBet(...)`); handlers/messageHandler.js:700 (`gradeBetRecord(bet.id, result, ..., 'Auto-graded from capper graphic', true)`); services/database.js:624-676 (`gradeBetRecord` — a bare `UPDATE bets`, no event/audit write)
- What / Why it matters: Both paths finalize `result` + bankroll with no `pipeline_events` row and no `grading_audit` row. Verified: the sweep block (grading.js:1893-1923) contains no `recordDrop`/`recordStage`/`writeAudit`; `autoGradeBet` (messageHandler.js:667-725) likewise; `gradeBetRecord` writes only `bets`. Consequence: `/api/admin/grader-health` (routes/admin.js:497-547, all three sections read bets-pending + grading_audit + search_backend_calls), `/api/admin/drops` (event_type='DROP'), `/admin pipeline-trace`, and `/admin pipeline-drops-24h` are ALL structurally blind to them. The probe pack's DP-01 (60 evidence-free sweep LOSSes in 30d, P0) was findable only via `grade_reason LIKE 'Auto-swept%'` — an operator watching the shipped dashboards would have seen a healthy grader for the whole month. This is the largest remaining class of the 06-10 audit's "terminal write with an empty trail" pattern (B7 closed it for unscoped voids only).
- Evidence: `grep -rn "GRADING_COMPLETE" services handlers commands` → zero emitters (enum-only); sweep loop grading.js:1893-1923 has only `console.log`; autoGradeBet body 667-725 has neither `recordStage` nor audit.
- Proposed fix: one `bets.recordDrop`-style emission per terminal write — e.g. `transitionTo({toStage:'GRADING_COMPLETE', eventType:'STAGE_EXIT', payload:{via:'sweeper_7d'|'graphic_auto', result}})` at both sites (fire-and-forget, mirrors GRADE_AUTOVOID_UNSCOPED's pattern at grading.js:2883). (Effort S)
- Backlog: NEW (adjacent to BACKLOG:503 parent-drop-reason aggregation)

### T6-02 [P1] [confidence: high] `auto_void_no_searchable_data` still finalizes with an empty pipeline trail — the CODEMAP-acknowledged "sibling gap" from 2026-06-16 remains open at HEAD
- Where: services/grading.js:1235-1260 (`autoVoidNoSearchableData` — direct UPDATE, no `bets.recordDrop`)
- What / Why it matters: The B7 follow-up (#111-era) instrumented the unscoped void (`GRADE_AUTOVOID_UNSCOPED`, grading.js:2883) and the unmodeled divert (`GRADE_MANUAL_REVIEW_UNMODELED`, :2828) but deliberately left the no-data void dark — CODEMAP.md:147 admits it: "the no-data void (`review_status='auto_void_no_searchable_data'`, no drop emitted — sibling gap)". This is the void class that was mass-mis-firing pre-#145 (probe pack: voids are 1678 of all terminal grades) and its per-void audit trail is unreconstructable forever. `/admin snapshot` shows only a 24h count from bets (commands/admin.js:664-666).
- Evidence: file read :1235-1260 — the only side effects are `console.log` + the UPDATE; the sibling paths at :2828/:2883 both have `bets.recordDrop`.
- Proposed fix: mirror the sibling: `if (res.changes > 0) bets.recordDrop({betId, stage:'GRADING_DROPPED', dropReason:<new registered reason>})` — needs a new DROP_REASONS entry (e.g. `GRADE_AUTOVOID_NO_DATA`) + CODEMAP §Enums. (Effort S)
- Backlog: NEW (CODEMAP flags it; no BACKLOG item exists)

### T6-03 [P2] [confidence: high] PRE_FILTER enforce decision: per-bucket FP labeling is derivable but UNCOMPUTED — the join exists in the schema, no surface runs it, and the labeled sample is ~43 rows with 4.3-day label lag
- Where: handlers/messageHandler.js:1268-1281 and 1367-1380 (shadow row: `stage:'PRE_FILTER_WOULD_DROP'`, `payload.bucket`, primary ingest_id only); migrations/025 (`hold_review_decisions.ingest_id`, `human_decision`, `bet_id`)
- What / Why it matters: **Decision answer (2b): NOT yet, but the gap is analysis + volume, not schema.** FP = "hold the filter would have killed, that a human later released into a real bet". Both halves exist: the shadow row carries `bucket` per held post, and `hold_review_decisions` records the human outcome keyed by the same ingest_id (`/holds` dedup keeps the primary `disc_<mid>` rep — routes/admin.js:125-133 — so keys align). Missing: (1) nothing computes `PRE_FILTER_WOULD_DROP ⋈ hold_review_decisions GROUP BY bucket, human_decision` — bucket rides payload JSON, so `/drops`' reason filter can't see it; (2) probe pack: only 43 would-drops since 06-24 vs 327 lifetime decisions at median 102.6h latency — most recent shadow rows are unlabeled; (3) med-confidence caveat: a multi-message hold recovered via a sibling ingest_id would mislabel an FP as a true-negative (shadow row is primary-only).
- Evidence: file reads above; probe pack 7e (43 would-drops: recap 28 / promo 14 / sweat 1) + probe 6 (decisions 249 dismissed / 68 recovered / 3 released_with_edits).
- Proposed fix (smallest closure): a read-only query (script or `?bucket=` variant on `/drops`) — `SELECT json_extract(pe.payload,'$.bucket') b, d.human_decision, COUNT(*) FROM pipeline_events pe LEFT JOIN hold_review_decisions d USING (ingest_id) WHERE pe.stage='PRE_FILTER_WOULD_DROP' GROUP BY 1,2` — and a rule: enforce a bucket only when it has ≥N decided rows with 0 recovered. No new write-path instrumentation needed; just let shadow accumulate ~3-4 more weeks. (Effort S)
- Backlog: extends #137 shadow item (docs/MEMORY.md pre-filter entry); NEW as an explicit "enforce gate read"

### T6-04 [P2] [confidence: high] EVENT_AWARE_RECHECK enforce decision: recheck-side telemetry is sufficient, but the sweep guard — the safety half of the flip — has zero shadow measurement
- Where: services/grading.js:1740 (`eventAwareRecheckMode() === 'enforce' && nextAttemptForEvent(...).defer` — enforce-only, no shadow emission); :1144/:1816 (the only two `emitEventAwareShadow` sites)
- What / Why it matters: **Decision answer (2a): the RPM half — YES.** BACKLOG:227's two prescribed reads exist and are populated (probe 7: 364 rows, 52 would_defer / 312 would_window since 06-18; grading_audit baseline 96-955 attempts/day) with per-decision `phase`/`reason` in payload — enforce would skip ~4 claims/day, a measured, small win. **The protection half — NO:** `evaluateSweep`'s `event_pending` skip never runs and never emits in shadow (comment at :1735-1736 keeps shadow byte-identical to off), so "how many sweeps would the guard have prevented" is unmeasurable from telemetry. Today that number is provably ~0 (probe DP-01: all 60 swept bets had NULL event_date → `defer=false` at :1026-1027), which itself is the decision-grade fact: flipping enforce buys almost no sweep protection until DP-02 (event_date coverage) improves. The actual flip blocker remains the code constant collision `MAX_DEFER_MS`(168h, :1009) == `SWEEP_CUTOFF_MS`(7d, :1691-1692), unchanged at HEAD. (BACKLOG:227 cites `:963`/`:1593` — line drift only.)
- Evidence: file reads above; probe pack 7/7c/7d.
- Proposed fix (smallest closure): in `evaluateSweep`, when mode==='shadow' and `plan.defer`, emit one `emitEventAwareShadow(bet.id, {kind:'sweep_would_skip_event_pending', ...})` before returning eligible — additive observational row, behavior unchanged; flip enforce only after that counter is nonzero AND MAX_DEFER_MS < SWEEP_CUTOFF. (Effort S)
- Backlog: BACKLOG:192 + :227 (existing enforce-flip item)

### T6-05 [P2] [confidence: high] pipeline_events grows unbounded: the "90-day purge" two modules are designed around does not exist anywhere in the codebase
- Where: bot.js:774-791 (the 3:30 UTC purge cron — bets/user_bets/processed_tweets/twitter_audit_log/bot_health_log only); migrations/025 header ("pipeline_events rows are purged on a 90-day window"); services/holdReview.js:106 (same claim)
- What / Why it matters: `grep -rn "DELETE FROM pipeline_events" --include='*.js'` across the repo → zero matches. So (a) the table grows forever — a plausible contributor to the 58MB DB + 58MB WAL (probe DP-07) since it is the highest-write-rate table (probe P5: ~8.9k drops alone in 14d), and every `/holds` request full-scans it for resolution state (routes/admin.js:182-195, only mig-031's event_type index exists); (b) mig 025's design rationale (hold_review_decisions as the durable copy "because events purge") is built on a false premise — harmless today, but if someone "fixes" the missing purge per those comments without checking `/holds`' dependence on old MANUAL_REVIEW_HOLD rows (226 open holds, 2 aged >30d), open holds would silently vanish from the queue.
- Evidence: grep exit=1 (no match); bot.js:774-791 read (five DELETEs, none pipeline_events).
- Proposed fix: decide the retention policy explicitly — either add a purge that exempts unresolved-hold ingest_ids (join before delete) or correct the two comments to "no purge exists"; add pipeline_events row-count to `/admin snapshot`. (Effort S)
- Backlog: NEW

### T6-06 [P2] [confidence: med] /admin snapshot backend health is per-process RAM wiped by the daily 4AM-ET restart — chronic serper/ddg failure reads as "idle/healthy" every morning (F-11 class, recommendation never shipped)
- Where: services/grading.js:2193-2198 (`backendHealth` in-memory); commands/admin.js:637-657 (`fmtBackend` renders only that snapshot); bot.js:795-800 (daily `process.exit(0)` at 8:00 UTC)
- What / Why it matters: The 05-22 audit's F-11 recommended a stale-data marker; what shipped (M-3, getBackendSnapshot states at grading.js:2249-2276) is honest *within a process lifetime* — but the process lives ≤24h by design, so `failCount/lastError` reset daily. A backend that is 100% dead for 14 days (probe DP-03: serper 115 calls 0 ok, ddg 0 ok) renders as `idle` after every restart and `failing (3 fails…)` at best — indistinguishable from a transient blip. The durable truth exists (`/admin search-backends`, commands/admin.js:1131-1197, windows to 168h; `/grader-health backends24h`) but nothing alarms: an operator must proactively run the command to discover that the evidence tier is down to Bing alone.
- Evidence: file reads above; probe pack DP-03 histogram.
- Proposed fix: fold a 7d `search_backend_calls` ok-rate per backend into `/admin snapshot`'s backend lines (one query), and DM the owner when any backend's 7d ok-rate is 0% with ≥20 calls. (Effort S)
- Backlog: 05-22 audit F-10/F-11 (recommendation half unshipped); probe pack DP-03

### T6-07 [P3] [confidence: high] pipelineHealth's startup "OK" is a tautology: it proves the three known drop stages fired, and structurally cannot detect the silent writers (T6-01/T6-02) it exists to catch
- Where: services/pipelineHealth.js:31 (`EXPECTED_STAGES = ['DROPPED','GRADING_AI','GRADING_DROPPED']`), :55-68; bot.js:556-562 (runs once per boot)
- What / Why it matters: The check warns only when an *expected* stage has zero drops in 24h — i.e., it can catch deleted instrumentation, never missing instrumentation. `PIPELINE_INSTRUMENTATION_OK` logs daily (thanks to the restart) while sweep/celebration/no-data-void write terminally in the dark — an operator grepping logs for the gap detector reasonably concludes coverage is complete. Also the provenance comments are stale: `:22-26` cite `grading.js:390` (GRADING_AI) and `:1633` (GRADING_DROPPED); actual sites are `:3183` and `:1106/:2828/:2883` + bets.js:109-118. The stage SET itself is still accurate (verified: all `bets.recordDrop` call sites use GRADING_AI or GRADING_DROPPED; ingest `dropAll`/`recordDrop` default 'DROPPED').
- Evidence: file reads above; grep of all recordDrop call sites.
- Proposed fix: rename the OK log to reflect scope (`known drop stages active`), refresh the comment line refs, and add a companion assertion driven off T6-01/T6-02's new emissions once they exist. (Effort S)
- Backlog: NEW

### T6-08 [P3] [confidence: high] Enum/CODEMAP drift bundle: three registered stages have zero emitters; CODEMAP's stage list omits PRE_FILTER_WOULD_DROP; BACKLOG references an unregistered REGRADE_PROMOTE stage
- Where: services/pipeline-events.js:28-33 (`GRADING_SEARCH`, `GRADING_GUARDS`, `GRADING_COMPLETE` registered); docs/CODEMAP.md:145 (stage list ends at the #109-era set); docs/BACKLOG.md:439 (regrade promote "logs to pipeline_events with stage='REGRADE_PROMOTE'")
- What / Why it matters: (a) grep across services/handlers/commands finds no emitter for GRADING_SEARCH/GUARDS/COMPLETE — a successfully graded bet's `/admin pipeline-trace` therefore ends at ingest + any drops, with no terminal event (grading_audit holds the truth, but the trace surface implies the enum is real). (b) CODEMAP.md:145 does not list `PRE_FILTER_WOULD_DROP` (in code at pipeline-events.js:26 since #137) — violates CODEMAP.md:147's own rule ("new values must be added to that array **and CODEMAP §Enums**"). (c) If the regrade skill's promote script writes `stage='REGRADE_PROMOTE'`, the warn-only tripwire fires on every row (unregistered) — noise that trains operators to ignore the drift warning.
- Evidence: greps + file reads cited above.
- Proposed fix: register or delete the dead grading stages (T6-01's fix would make GRADING_COMPLETE real), add PRE_FILTER_WOULD_DROP to CODEMAP:145, register REGRADE_PROMOTE (or correct BACKLOG:439). (Effort S)
- Backlog: NEW (docs-sweep class, cf. #123)

### T6-09 [P3] [confidence: high] Operator review actions (approve / revert / reject) leave no pipeline_events trail — pipeline-trace cannot reconstruct why a bet re-entered or left the grading queue
- Where: services/database.js:728-742 (`revertBetToPending`), :971-991 (`approveBet`), :994-997 (`rejectBet`) — bare UPDATEs
- What / Why it matters: The hold-side equivalents all emit (MANUAL_REVIEW_RELEASED/DISMISSED, holdReview.js:119/:309); the bet-side review queue emits nothing, so a trace over an incident like 453e0952 (the exact revert-race class #118/#157 guard) has a hole where the human acted. `/grade override` (#150) at least writes `bet_grade_history`; approve/revert/reject write only mutable bets columns (revert reason overwrites `grade_reason`).
- Evidence: file reads above; `grep recordStage|transitionTo` in those functions → none.
- Proposed fix: one `recordStage({sourceType:'manual', stage:'GRADING_ENTER', eventType:'STAGE_ENTER', payload:{action, actor}})` per action. (Effort S)
- Backlog: NEW

## Looked good
- F-17 closure holds at HEAD: the three relay-image terminal DROPs (`VISION_RESULT_RECAP`/`VISION_UNTRACKED_WIN`/`VISION_TICKET_RECAP`, messageHandler dropAll sites) + `PRE_FILTER_AI_EMPTY_RESULT` are registered (pipeline-events.js:61,73-75) and live-emitting (probe P5: 103/23/-/3 in 14d); the warn-only write-boundary enum tripwire (#49) is present at the single write boundary (pipeline-events.js:154-169, called :181) and the probe found zero unregistered drop_reasons in 14d.
- `DROP_REASONS` is a genuinely closed, commented source of truth (pipeline-events.js:47-100); grading-side earlyReturn classifies every forced-PENDING to a registered reason with a payload-preview (grading.js:3170-3202).
- #161 endpoints are honestly built: `/drops` filters on `event_type='DROP'` (not a stage list) with the epoch-seconds quirk handled as a bound param (routes/admin.js:460-462); `/grader-health` handles the three timestamp conventions correctly (:492-500); mig-031 index confirmed present live (probe P0).
- The two shadow measurement channels for the enforce decisions ARE emitting with decision-grade payloads: `event_aware_shadow` kind/phase/reason/wouldNext (grading.js:1144,1816), `PRE_FILTER_WOULD_DROP` bucket/sample/messageUrl (messageHandler.js:1268-1281) — the gaps are analysis surfaces, not write-path instrumentation (T6-03/T6-04).
- Ingest stage coverage is real: RECEIVED→…→VALIDATED→STAGED all emit (22 call sites across messageHandler/twitter-handler), and hold lifecycle events pair with durable `hold_review_decisions` rows keyed by the same ingest_id.

## UNVERIFIED / open questions
- Whether the running Fly image (v756-758) matches HEAD for routes/admin.js — #161 merged 2026-07-01; endpoint behavior in prod not probed (deploy is manual per project memory).
- Whether the zonetracker-regrade promote script actually writes `stage='REGRADE_PROMOTE'` rows (BACKLOG:439's claim) — script lives under skills/ and was not executed/read end-to-end this track (T6-08c is conditional on it).
- The multi-ingest-id FP-mislabel caveat in T6-03 (shadow row on primary vs decision on sibling id): rep-selection at routes/admin.js:125-133 prefers the primary, but the review-holds *script* path's keying was not traced; label a few real rows before trusting the join blindly.
- Whether `/holds`' per-request full scans of pipeline_events (stage filter has no supporting index; only mig-031's event_type index exists) are a measurable event-loop cost at the current ~226-hold, multi-MB table size — flagged in T6-05, not benchmarked (no node in worktree, no prod probe granted for EXPLAIN).
