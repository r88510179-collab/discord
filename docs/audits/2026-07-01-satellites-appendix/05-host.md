> Track analysis produced by a read-only audit subagent from pinned clones + probe captures;
> reviewed, spot-verified (high-severity evidence lines re-read by the orchestrator), and filed
> by the audit orchestrator. Probe references (P-n, probes/*.txt) resolve to 20-live-probes.md.

# T5 — Surface Pro host audit (2026-07-02 capture; box boot 2026-06-29 00:24 ET)

## Exposure table

| port | iface | process | tailscale mapping | verdict | assessment |
|---|---|---|---|---|---|
| 443 | 100.89.87.116 + [fd7a:…:57bb] (tailscaled) | tailscaled funnel frontend → 127.0.0.1:11435 `ollama-proxy` | Funnel (exposure.txt:27-28) | **PUBLIC** | Intended. Token gate verified live: no-secret → 401 (health-curls.txt:7-8). Continuous internet-scanner 401 noise in proxy logs; real client IP is NOT logged — every hit logs `from 127.0.0.1` (tails-ocr-proxy.txt out-0.log lines, → HO-9). |
| 8443 | 100.89.87.116 + v6 (tailscaled) | tailscaled funnel → 127.0.0.1:11436 `zonetracker-ocr` | Funnel (exposure.txt:30-31) | **PUBLIC** | Intended. Bearer-token gate; scanner probes (`/actuator/env`, `/.vscode/sftp.json`, wp-json, `/metrics` from 178.128.207.138, 142.93.143.8, 198.235.24.x, 205.210.31.x…) all 404 (tails-ocr-proxy.txt:6-12,84-85,188). Proves public scanners demonstrably reach this box's funnel ports. |
| 8444 | 100.89.87.116 + v6 (tailscaled) | tailscale **serve** → 127.0.0.1:8787 dashboard | serve, tailnet only (exposure.txt:33-34) | tailnet | Intended — deliberately not Funnel; matches SURFACE-PRO.md:39. |
| 11434 | `*` (ALL interfaces) | `ollama` (systemd, pid 1263; confirmed live `"Ollama is running"` v0.20.5, health-curls.txt:10-11) | none | **LAN + tailnet, UNAUTHENTICATED** | **Finding HO-1.** exposure.txt:14 `*:11434`. The proxy's secret gate covers only the Funnel path; any LAN or tailnet peer bypasses it entirely. |
| 22 | 0.0.0.0 + [::] | sshd (attribution standard/inferred; unit not in probe) | none | LAN + tailnet | Admin access (Tailscale SSH also in use per SURFACE-PRO.md:4). Auth posture (PasswordAuthentication) UNVERIFIED. Consider tailnet-only bind/firewall if the LAN is not trusted. |
| 11435 | 127.0.0.1 | socket held by `PM2 v6.0.14: Go[d]` pid 1389 (exposure.txt:10) — cluster-mode socket sharing for `ollama-proxy` | funnel backend | loopback | OK. God-daemon-held socket is the cluster-mode signature (see snapshot notes). |
| 11436 | 127.0.0.1 | `python` pid 1538 (`zonetracker-ocr`) | funnel backend | loopback | OK. Binds loopback since Jun 29 boot (tails-ocr-proxy.txt error-3.log: "Uvicorn running on http://127.0.0.1:11436"). Note an old Jun 2 startup line shows a brief `0.0.0.0:11436` era — since fixed. |
| 8787 | 127.0.0.1 | `node` pid 1543 (dashboard) | serve backend | loopback | OK. |
| 45723 / v6 37554 | 100.89.87.116 / [fd7a:…] | tailscaled internal (peerapi) | n/a | tailnet | Normal tailscaled service ports. |
| 631 | 127.0.0.1 + ::1 | CUPS | none | loopback | Hygiene: printing stack on a headless server; disable if unused. No exposure. |
| 53 | 127.0.0.54, 127.0.0.53%lo | systemd-resolved | none | loopback | OK. |

**Verdict:** the public surface is exactly the two intended, token-gated Funnel paths — no unexpected public listener. Live doc SURFACE-PRO.md:37-43 exposure map matches `tailscale serve/funnel status` byte-for-byte (no drift there). The only beyond-loopback *unauthenticated* service is ollama `*:11434` (HO-1).

## Findings

**PM2/host snapshot context (coverage 1/2/4/6).** pm2-list.txt: all 5 apps online, ↺0 except dubclub; uptimes 3D = Jun 29 00:24 boot (systemd boot-resurrect worked; systemd.txt: both units active since 00:24:39/40). dubclub ↺12 over 3.35d ≈ **3.6/day — improved ~2× vs June's ↺4/13h (≈7.4/day)** and exactly accounted for: 11 "IMAP connection closed. Exiting so PM2 can restart" exits post-boot + 1 boot-time `FATAL: getaddrinfo EAI_AGAIN imap.gmail.com` at 00:24:43 (tails-pm2-logs.txt:86-92) = 12. Rate is normal for its crash-only reconnect design, not worsening. ocr `version N/A` is cosmetic (Python app, no package.json for pm2 to read). ollama-proxy runs cluster mode — matches SURFACE-PRO.md:25, caused by `instances: 1` without `exec_mode: 'fork'` in its example config (zonetracker-ollama-proxy@62b8c6b ecosystem.config.example.js:10) — the exact PM2 default-to-cluster footgun the scraper/dubclub configs explicitly guard against (BACKLOG.md:73); harmless for a stateless proxy, but inconsistent. Disk: 18% used / 183G free (logs-disk.txt:17) vs June 17%/184G — ~1G/3wk trajectory, years of headroom. systemd: pm2-tracker tree 1.7G mem (peak 2.2G), 211 tasks vs 7951 limit (2.7%) — ample; CPU 20h39m/3.34d ≈ 26% of one core, dominated by Playwright cycles (a full scraper cycle is 105s per tails-repo-logs.txt:200), acceptable.

---

### HO-1 · P2 · confidence high — Ollama `*:11434` unauthenticated on ALL interfaces (June's "observation" upgraded to a finding)
**Evidence:** exposure.txt:14 (`LISTEN … *:11434`); health-curls.txt:10-11 (unauthenticated root → `"Ollama is running"`, `{"version":"0.20.5"}`); SURFACE-PRO.md:197-199 concedes it ("`:11434` itself is reachable on the LAN/tailnet without that gate. Observation only"); systemd.txt:3-4 (override.conf drop-in exists — presumably where the bind is set; contents UNVERIFIED); SURFACE-PRO.md:62-63 (proxy forwards to hardcoded `http://127.0.0.1:11434`, so a loopback re-bind breaks nothing).
**Impact:** any device on the home LAN (guest/IoT included) or any tailnet node can use the standard Ollama API without the proxy's secret — mutating and costly operations (model delete would break the bot's live `llama3.2:3b` grading tier; model pulls fill disk; generate calls starve the CPU that scraper/dubclub/ocr share). The June audit deferred this as an observation; the same capture now demonstrates public scanners actively probing this box's other ports (tails-ocr-proxy.txt:6-12), so defense-in-depth around the one unauthenticated service is warranted. Not P0 because :11434 itself is not Funnel-mapped (not internet-reachable).
**Fix:** set `OLLAMA_HOST=127.0.0.1` in `/etc/systemd/system/ollama.service.d/override.conf`, `daemon-reload` + restart, then curl `100.89.87.116:11434` (should fail) and re-run a bot `x-ollama-secret` probe through the Funnel (should still 200). **Effort:** S. **BACKLOG:** NEW (host section; upgrades SURFACE-PRO.md:197-199 note; closes part of June H-class).

### HO-2 · P2 · confidence high — Log-location chaos: stale files + wrong-id suffixes will deceive an operator mid-incident
**Evidence:** three coexisting conventions across 5 apps: (a) dubclub/scraper flat custom files, (b) repo-local id-suffixed files, (c) ~/.pm2 id-suffixed files. Specifics: `~/.pm2/logs/zonetracker-scraper-out.log` is 15M and **frozen at Jun 10 12:36** (logs-disk.txt:10 mtime), and its tail *ends on error lines* — "`[Fetch] @nrfianalytics error: page.waitForSelector: Timeout…`" / "`[Disable] @nrfianalytics hit 5 strikes`" (tails-pm2-logs.txt:683-694) — while the real, live log is `~/zonetracker-scraper/logs/out.log` (cycle running Jul 2 08:35-09:16, tails-repo-logs.txt:1-12,195-200). ocr writes `logs/out-3.log` (live: our Jul 2 healthz curls) while its pm_id is now **1** and `logs/out-1.log` does not exist (tails-ocr-proxy.txt:1-5); its committed config says plain `./logs/out.log` (zonetracker-ocr@e21ee2c ecosystem.config.js:15-16) which **also does not exist on disk** (tails-repo-logs.txt:403-406). ollama-proxy has both `out-0.log` (live) and a dead `out-1.log` (last write Jun 25) side-by-side (tails-ocr-proxy.txt:255,457). dashboard writes `~/.pm2/logs/zonetracker-dashboard-*-4.log` while pm_id is now **2**; its error file additionally has **no timestamps** on `[proxy] … ERROR (TimeoutError)` lines (tails-pm2-logs.txt:403-420, mtime Jun 25 — T4's bug, noted here as log-hygiene).
**Impact:** operator deception. During an incident, tailing the file that matches the app's *current* pm_id finds nothing (ocr `out-1.log` missing), tailing the obvious `~/.pm2/logs/zonetracker-scraper-out.log` shows 3-week-old data ending on what looks like a live outage, and dead twins (`out-1.log` beside `out-0.log`) invite reading the wrong month. The id suffixes come from the PM2 dump freezing the log path with the pm_id at registration time — and pm_ids churn on every delete/start env cycle (see HO-6).
**Fix:** one convergence pass: `pm2 delete <app> && pm2 start <ecosystem file> && pm2 save` per app (paths regenerate), standardize every ecosystem file on explicit unsuffixed `error_file`/`out_file` + `merge_logs: true` (the scraper example already does this — zonetracker-scraper@ff1a906 ecosystem.config.example.js:23-25), delete/archive the stale `~/.pm2/logs/zonetracker-scraper-*` and dead `out-1.log`, and pair with the HO-4 logrotate install. **Effort:** S. **BACKLOG:** extends H-1/S-04 (June audit :352-354, :372-374); the deception angle is NEW.

### HO-3 · P2 · confidence high — Scraper box never got PR #5: secret-bearing `ecosystem.config.js` still tracked + locally modified = commit hazard and a guaranteed pull conflict
**Evidence:** checkouts.txt:1-6 — box at `ff9fda0` (one behind origin `ff1a906` = PR #5 "eco-untrack", merged 2026-06-10) with ` M ecosystem.config.js` + `?? ecosystem.config.js.bak.20260610-115918`. At origin HEAD the file is deleted from tracking, gitignored, and replaced by an example (clone: merge stat of ff1a906 shows `ecosystem.config.js | 19 ---`; .gitignore:158 `ecosystem.config.js` with the comment "PM2 config carries the live ALERT_WEBHOOK_URL — keep it local, ship the example"). The tracked version at box HEAD carries only `NODE_ENV` (git show ff9fda0:ecosystem.config.js), while SURFACE-PRO.md:95-98 documents the live block carries the **real Discord `ALERT_WEBHOOK_URL` + `DEAD_AIR_CYCLES`** — i.e. the local modification is the secret.
**Impact:** (a) a `git commit -a`/`git add -A` on the box publishes the live alert webhook to the repo — the exact leak PR #5 was written to prevent, still open on-box 22 days after merge; (b) the next `git pull` hits a modify/delete conflict, and both naive resolutions are traps: `checkout/reset --hard` silently destroys the only copy of the live env (disarming the S-01 dead-air alarm on the next delete/start cycle), while committing "resolves" by leaking; (c) the standing footgun that `pm2 restart` never re-reads the env block (memory + SURFACE-PRO.md:69-72) means file↔dump divergence is invisible until the next delete/start.
**Fix:** deploy PR #5 deliberately: `cp ecosystem.config.js /tmp/eco.live && git checkout -- ecosystem.config.js && git pull && cp /tmp/eco.live ecosystem.config.js` (now untracked+ignored), delete the `.bak`, then `pm2 delete zonetracker-scraper && pm2 start ecosystem.config.js && pm2 save` to converge file/dump/runtime. While in there, add `exp_backoff_restart_delay` (S-04 residual: example still lacks it — ecosystem.config.example.js:27-28 has only `max_restarts: 10`/`min_uptime: '60s'`). **Effort:** S. **BACKLOG:** scraper PR #5 shipped-but-not-deployed gap (SHARED baseline confirms "merged Jun 10, NOT deployed"); S-04 residual.

### HO-4 · P3 · confidence high — H-1 verdict: REGRESSED/OPEN (second consecutive audit). No pm2-logrotate; ~/.pm2/logs 41M → 46M, and 46M understates real growth
**Evidence:** logs-disk.txt:2-14 — `~/.pm2/logs` 46M: dubclub-error **20M** (June 16M), dubclub-out **13M** (June 11M), scraper-out 15M **stale since Jun 10 12:36**; `~/.pm2/modules` is EMPTY, dir mtime **Apr 9** (logs-disk.txt:12-14). June baseline: audit :372-374 (41M, "contains no logrotate module").
**Assessment of June's "(only Module: ollama-proxy)" (audit :507):** nothing was lost in the reboot — an installed pm2 module creates a subdirectory under `~/.pm2/modules`, and that dir is empty with an April mtime, so no module has been installed at any point since Apr 9. June's F.9 parenthetical was almost certainly a misreading of the apps table (ollama-proxy is a regular app). H-1 stands: **no rotation has ever existed**.
**Impact:** all measurable growth (+6M/22d ≈ 270KB/day) is dubclub, whose error log is precisely the incident-forensics file for the live GuessAndPrayBets login-wall incident; true total growth is higher — the active scraper/ocr/proxy logs live in repo-local `logs/` dirs whose sizes the probe whitelist could not capture (UNVERIFIED). Disk is safe for years (18%), but greps get slower and the growth is unbounded.
**Fix:** the same one-liner two audits have asked for: `pm2 install pm2-logrotate && pm2 set pm2-logrotate:max_size 10M && pm2 set pm2-logrotate:retain 14 && pm2 set pm2-logrotate:compress true` (also closes the log halves of D-4/S-04/U-6). **Effort:** S. **BACKLOG:** H-1 (June audit :372-374) — reaffirm, now with the "third time filing this" flag.

### HO-5 · P3 · confidence med — dubclub's canonical env AND restart policy exist only on-box (untracked `.cjs`, no committed example) — and the Jun 23 EAI_AGAIN storm shows why that's not academic
**Evidence:** checkouts.txt:9-12 (`?? ecosystem.config.cjs` untracked); dubclub repo at 633d084 contains **no ecosystem file and no example** (git ls-files: only `.env.example`) — unlike scraper and ollama-proxy, which both commit CHANGEME examples. SURFACE-PRO.md:121-125 declares the `.cjs` the canonical env source (closing U-6). Storm: tails-pm2-logs.txt:3-71 — **9 `FATAL: getaddrinfo EAI_AGAIN imap.gmail.com` crashes in 12 seconds** (Jun 23 18:23:51→18:24:03), i.e. instant restarts with no backoff during a DNS blip.
**Impact:** (a) single point of loss — a disk failure or careless `git clean -fdx` erases the only copy of the canonical env + restart policy with nothing to restore from; (b) the restart policy is unreviewable from the repo: if it mirrors the scraper's (`max_restarts: 10` / `min_uptime: '60s'`, no `exp_backoff_restart_delay` — the S-04 pattern), the Jun 23 storm stopped **one restart short** of PM2 parking dubclub `errored` permanently with no alert, and the actual values are UNVERIFIED precisely because the file is untracked.
**Fix:** commit `ecosystem.config.example.cjs` (CHANGEME placeholders, keys already documented in SURFACE-PRO.md:121-125) including `exp_backoff_restart_delay: 1000`; apply on-box via the delete/start/save cycle. **Effort:** S. **BACKLOG:** H-2 class residual + S-04's backoff item ported to dubclub; NEW as a concrete dubclub-repo task (cross-ref T3).

### HO-6 · P3 · confidence high — SURFACE-PRO.md doc-drift: the pm_id map is 100% wrong (all five ids), plus two stale HEAD lines
**Evidence:** SURFACE-PRO.md:23-29 "At a glance" says ollama-proxy=1, scraper=0, dubclub=2, ocr=3, dashboard=4. Live (pm2-list.txt): ollama-proxy=**0**, ocr=**1**, dashboard=**2**, dubclub=**3**, scraper=**4**. Also stale: dubclub HEAD "b55c449" (:111) — box is at 633d084 (checkouts.txt:8); dashboard HEAD "b37e51a" (:174) — box is at d392754 (checkouts.txt:16).
**Impact:** every pm_id in the "authoritative" host inventory is wrong; an operator running `pm2 restart 3` off the doc restarts **dubclub, not ocr**. Root cause is structural: pm_ids are reassigned on every `pm2 delete/start` env cycle (the documented deploy procedure itself churns them), so an id column can never stay accurate.
**Fix:** refresh the table AND replace the id column with a "pm_ids churn on delete/start — always address apps by name" caveat; update the two HEAD lines while in there. **Effort:** S. **BACKLOG:** doc-drift, NEW (SHARED rule: code/live contradicting SURFACE-PRO.md is itself a finding).

### HO-7 · P3 · confidence med — Boot-order race: pm2-tracker resurrects apps before DNS is ready (dubclub FATAL 3s after service start)
**Evidence:** systemd.txt:16 (pm2-tracker active since Jun 29 00:24:40); tails-pm2-logs.txt:86-92 (dubclub `FATAL: getaddrinfo EAI_AGAIN imap.gmail.com` at 00:24:43). Self-healed via pm2 autorestart (dubclub online since).
**Impact:** benign this boot, but combined with no restart backoff (HO-5) a slower network bring-up (Wi-Fi renegotiation, router reboot alongside box reboot — e.g. after a power cut) turns the boot race into an instant 10-crash burn → `errored` park while unattended. ollama.service has an override.conf whose contents are UNVERIFIED; pm2-tracker.service ordering directives likewise UNVERIFIED.
**Fix:** add `Wants=network-online.target` / `After=network-online.target` to pm2-tracker.service (and/or in-app: treat boot-window EAI_AGAIN as retry-with-delay instead of FATAL exit). **Effort:** S. **BACKLOG:** NEW.

### HO-8 · P3 · confidence med — H-3 verdict: mixed — (a) partial, (b) partial, (c) REGRESSED (bak accumulation)
**Evidence & sub-verdicts** (June audit :380-381):
- **(a) value-dumping commands / secret rotation — PARTIAL.** The never-run guidance (`pm2 jlist`, `pm2 conf`) lives only inside the June audit file; no runbook carries it (grep across docs/ hits only SURFACE-PRO.md:10-11's parse-discipline note, which predates and omits `pm2 conf`). `OLLAMA_PROXY_SECRET` rotation since it transited a terminal: UNVERIFIED (needs box file mtime + Fly secrets digest).
- **(b) undocumented dirs — PARTIAL.** `~/zonetracker-stats` is now documented as cron-only in SURFACE-PRO.md:201-209 and its full inventory pass remains open (BACKLOG.md:667) — acceptable. The `~/zonetracker` dead 97M checkout: UNVERIFIED (probe whitelist had no home-dir listing); presumed still present.
- **(c) bak hygiene — REGRESSED.** dubclub still has `config.json.bak-20260610` and has **gained** `storageState.json.bak-20260615` (checkouts.txt:10-12); scraper carries `ecosystem.config.js.bak.20260610-115918` (checkouts.txt:4). Live config drift is real, not theoretical: dubclub logs `UID 11336: capper "ZoeLab" not in config.json. Skipping` every sweep (tails-pm2-logs.txt) while `config.json` sits modified with a stale bak beside it — exactly the drift-trap H-3(c) predicted.
**Fix:** delete confirmed-stale baks during the HO-3/HO-5 convergence pass; add `pm2 conf` to a committed never-run list (RUNBOOKS); verify/rotate the proxy secret. **Effort:** S. **BACKLOG:** H-3 reaffirm.

### HO-9 · P3 · confidence high — ollama-proxy logs every public hit as `from 127.0.0.1`: zero client-IP forensics on the public Funnel
**Evidence:** tails-ocr-proxy.txt out-0.log/out-1.log — all entries, scanner storms included, read `401 GET /sender/.env from 127.0.0.1` etc.; tailscaled terminates Funnel TLS and proxies from loopback, so the proxy only ever sees 127.0.0.1. Contrast: the OCR service's uvicorn log DOES show real scanner IPs on the same box (tails-ocr-proxy.txt:6-12), so the incident-forensics asymmetry is proxy-specific.
**Impact:** during a real credential-stuffing or secret-leak incident on the LLM path there is no way to distinguish scanner vs Fly bot vs tailnet peer by source, or to correlate with the OCR log's IPs.
**Fix:** log the forwarded-client header tailscaled's serve/funnel proxy injects (verify header name on-box — expected `X-Forwarded-For`) alongside the loopback peer address in proxy.js. **Effort:** S. **BACKLOG:** NEW (overlaps T1's ocr/proxy beat — dedupe at synthesis).

## UNVERIFIED list

| What | Why blocked | Command that would obtain it |
|---|---|---|
| Sizes of active repo-local `logs/` dirs (scraper/ocr/proxy real growth since Jun 10) | probe whitelist had no `du` on repo dirs | `du -sh /home/tracker/{zonetracker-scraper,zonetracker-ocr,ollama-proxy,zonetracker-dubclub,zonetracker-dashboard}/logs` |
| `ollama.service` override.conf contents (where the `*:11434` bind is set) | not in probe set | `systemctl cat ollama.service` |
| `pm2-tracker.service` unit ordering (`After=network-online`?) | not in probe set | `systemctl cat pm2-tracker.service` |
| Box scraper `ecosystem.config.js` local-diff content (which keys were added; whether `exp_backoff_restart_delay` present) | read would echo a live webhook value — names-only discipline | `git -C ~/zonetracker-scraper diff ecosystem.config.js \| grep -oE "^[+-] *[A-Z_]+:"` (key names only) |
| dubclub `ecosystem.config.cjs` restart policy (`max_restarts`/`min_uptime`/backoff) | file is on-box only, value-bearing | `grep -E "max_restarts\|min_uptime\|exp_backoff\|autorestart" ~/zonetracker-dubclub/ecosystem.config.cjs` |
| PM2 dump ↔ file ↔ runtime env convergence (full H-2 check) | `pm2 env`/`pm2 jlist` echo values — never-run | key-name-only remote filter, e.g. `pm2 jlist \| jq '.[].pm2_env.env \| keys'` reviewed before running (H-3a caveat applies) |
| `~/zonetracker` dead 97M checkout still present; home-dir inventory | no home listing in probes | `ls -la ~ && du -sh ~/zonetracker*` |
| Current `crontab -l` (zonetracker-stats entry unchanged?) | not in probe set | `crontab -l` |
| sshd auth posture (PasswordAuthentication) | not in probe set | `sudo sshd -T \| grep -iE "passwordauth\|permitroot"` |
| `OLLAMA_PROXY_SECRET` rotated since Jun 10 (H-3a) | needs box+Fly correlation | box: `stat -c %y ~/ollama-proxy/ecosystem.config.js`; Fly: `fly secrets list` (digest dates) |
| Installed ollama model list (blast radius of unauth delete/pull) | not in probe set | `curl -s 127.0.0.1:11434/api/tags \| jq '.models[].name'` |

## Looked good

- **Boot-resurrect:** clean. Both systemd units and all 5 PM2 apps up since the Jun 29 00:24 boot; ocr model reloaded in 1035ms at 00:24:43 (tails-ocr-proxy.txt error-3.log), `/healthz` `{ok:true,modelLoaded:true}` (health-curls.txt:2).
- **dubclub restart rate:** ↺12/3.35d ≈ 3.6/day, fully explained by design-intended IMAP-exit reconnects + one boot blip — **improved ~2× vs June's ≈7.4/day**, not worsening. Browser watchdog arms on every boot (tails-pm2-logs.txt "Browser watchdog armed…").
- **Public exposure discipline:** only the two intended Funnel paths are public; both token gates verified against live scanner traffic (401s on proxy, 404s on ocr — nothing leaked); dashboard correctly serve/tailnet-only; ocr and dashboard bind loopback; the Funnel/serve map in SURFACE-PRO.md:37-43 matches live status exactly.
- **S-01 observability demonstrably works in production:** scraper error.log shows ISO-stamped `[Strike]`/`[Disable]` lines through the Jun 23 timeout storm, and cycles are healthy on Jul 2 ("Browser launched, 13 cookies loaded", "[Handles] fetched 8 active from Fly", cycle complete 105s) — the PR #3/#4 deploy is alive on-box.
- **Resource headroom:** disk 18%/183G (≈1G/3wk trajectory); pm2 tree 1.7G/peak 2.2G with 211/7951 tasks; ollama.service idle at 45.8M. Nothing trending toward exhaustion.
- **Checkout convergence for 3 of 5 services:** dashboard, ocr, ollama-proxy all clean at origin HEAD (checkouts.txt) — the H-2 class is now scraper/dubclub-only.
