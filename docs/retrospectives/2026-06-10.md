# Retrospective — 2026-06-10

> First file in `docs/retrospectives/`. The **morning** close-out (Gate 3 →
> enforce, event_date validation #70/mig-029, quarantine reset, Surface Pro
> S-01/U-1/D-1) is recorded as the `## ✅ SHIPPED — 2026-06-10 (ops close-out…)`
> block in `docs/BACKLOG.md` and the `audit/coa-full-2026-06-10` branch
> (`docs/audits/2026-06-10-coa-full-audit.md`). This file is the **evening
> addendum**: the #73/#74 ship and the pool reset.

## Evening ship — v606

Two grader correctness fixes merged and deployed together as **v606** (built
from clean `main`, `fly deploy --no-cache`, ~18:40Z):

### #73 (`31fd814`) — grade COMPLETE 1-leg parlays
A single pick stored as a 1-leg parlay (one `parlay_legs` row) was permanently
ungradeable: the ≤1-leg dispatch guard rejected every parlay with ≤1 recorded
leg. New pure helper `parlayLegDataComplete(description, legCount)` distinguishes
a **complete** 1-leg parlay (description names exactly one pick — one `•` — and
one leg exists) from genuine missing legs. The guard now skips only when *not*
complete; the missing-legs / 0-leg rejection and its exact reason string are
byte-identical, and multi-leg (≥2) parlays are untouched. 14 unit tests.

### #74 (`4c992c9`) — search backend honesty (M-3) + query ordinal fix
Every backend (Bing/Brave/DDG/Serper) now routes parsed results through
`assessSearchResults` before recording success: `parse_empty` (0 usable hits)
registers a circuit failure for *every* backend; Bing-only `generic_news`
(parsed but no hit mentions a query token) falls through *without* tripping the
breaker. `recordBackendResult` stamps `lastSuccess` only on a real success, so
`getBackendSnapshot` / `/admin` stop scoring drifted 200s as healthy. Bing/Serper
stay un-gated → Bing-first chain order preserved. Part 2: `extractSubject`
sentinel-protects ordinal/period qualifiers (`1st`–`4th`, `1H`/`2H`, `1Q`–`4Q`,
`F5`) so `"1st Quarter"` survives the numeric strip. 48 unit tests + patched
`search-chain-order` (8).

## Verification evidence (live, post-deploy)

- **Search fall-through works:** a real autograde query took the **Bing
  `GENERIC_NEWS` → Brave `SUCCESS`** path — junk-Bing reaching a working backend
  instead of being scored healthy, exactly the M-3 intent.
- **1-leg parlays grade:** three Group-B published-VOID resets (from the morning)
  graded on their first post-deploy cycles —
  - `ee2f755d…` New York Yankees ML → **loss**
  - `a1f9255b…` Colorado Avalanche ML → **win**
  - `f71cbbc5…` Marlins ML → **loss**
  - counter-specimen `7b04366b…` (0 bullets / 1 leg) **stays rejected**, reason string unchanged.

## Pool reset — decision & rationale

**298** bets in `grading_state='backoff'` had `grading_attempts` reset to **0**
(read-only-guarded write; pool-size guard 250–350; `grading_next_attempt_at`
left untouched). Rationale: those counters accrued during the **broken-search
era** — 264 of the 298 were at ≥5 attempts and would auto-void after just 1–2
*honest* tries. Resetting lets the now-honest search pipeline (S2/#74) judge each
bet on real source availability rather than inheriting burned counters. This is
also a precondition for **S1 measurement**, which should run after ~1 week of
honest attempts so the by-source cut is trustworthy.

Two write attempts during the session **aborted by design before touching a
row** (wrong column name; an 8-char-truncated bet id matching 0 rows). They are
written up as worked examples in `docs/RUNBOOKS/db-interventions.md` — the
origin of the two hard rules there (never trust a truncated id; never assume a
column name).

## Watch items

- **Gate 3 false blocks.** Enforce went live this morning; the would-fire review
  showed zero false positives on 7 bets, but enforce is now *blocking*, not
  shadowing. Watch `/admin` and `grading_audit` for any correctly-grading bet
  forced to PENDING with `UNVERIFIED_QUOTE`.
- **Pool drain rate.** With honest search live and the 298-bet reset, the
  `backoff` queue should now *drain* (grade or honestly void) rather than churn.
  If it stays flat or re-inflates, the search arc's per-sport adapters (S3/S4)
  are the real bottleneck, not the breaker.
- **VOID-slip flow as searchless bets exhaust honestly.** Bets with no source
  path will now exhaust *honestly* (1–2 tries → no data → eventual void) instead
  of churning forever. Expect a wave of legitimate `auto_void_no_searchable_data`
  / `GRADE_BACKOFF_EXHAUSTED` as the backlog clears — this is intended (S5), not
  a regression. Note the **non-uniform auto-void rule** (BACKLOG): the two void
  paths key on different signals, so the void wave won't be a clean function of
  attempt count.

## Follow-ups opened (BACKLOG)

- Search query builder slash/dash artifacts (`"McGhee/Yannis ITD"` → fused
  token; stray ` - ` dash) — P3. **→ shipped same evening in #76 (batch 2 below).**
- Bing `b_algo` defensive multi-selector parse — the structural Bing-parse fix
  deferred from #74 (today's mitigation is fall-through, not a better parse).
  **→ shipped same evening in #76 (batch 2 below).**
- Non-uniform auto-void rule — documented as an S5 exhaustion-policy input.

---

## Evening batch 2 — v610 (#76 grading + #77 ROI; LockedIn swap; S1a)

A second evening session shipped two more PRs (deployed together as **v610
~21:30Z**), completed the LockedIn handle swap end-to-end, and ran the first
search-arc S1 classification. Full file/line map: `docs/CODEMAP.md`; backlog
entries: `docs/BACKLOG.md` §"SHIPPED — 2026-06-10 evening batch 2".

### Ship list

- **#76 (`7a55842`) — query-builder artifacts + defensive Bing parse + dead
  resolver env.** Closed *both* follow-ups opened earlier the same evening:
  `extractSubject` now spaces slash/backslash (`"McGhee/Yannis ITD"` →
  `"McGhee Yannis ITD"`) and drops orphan dash-runs (`"Joanderson Brito ML (-165)"`
  → `"Joanderson Brito"`) while keeping intra-word hyphens; and the rotted single
  `b_algo` selector is replaced by pure `parseBingHtml(html)` (ordered
  block-delimiter × ordered title/snippet fallback, exported via `_internal`). A
  total parse miss still returns `[]` → `parse_empty` → honest fall-through to
  Brave (the S2 gate is **not** weakened). `fly.toml` `RESOLVER_URL`/
  `RESOLVER_VERSION` removed; the `zonetracker-resolver` app was **destroyed**.
  Tests: `tests/query-builder-bing-parse.test.js` (37).
- **#77 (`3ed77e2`) — unify + correct the capper ROI formula.** ROI is now
  defined **once** in `CAPPER_STATS_COLUMNS` (shared verbatim by `getCapperStats`
  + `getLeaderboard`), the arbitrary per-bet `MAX(units,1)` floor is removed, the
  numerator/denominator read the same settled set, `CAST(units AS REAL)`
  neutralizes text-garbage stakes, and division is `NULLIF`+`COALESCE`-guarded.
  Tests: `tests/capper-roi.test.js` (10).
- **dubclub#3 (`23c63ed`) — GNP leg-drop fix**, merged + deployed box-side (pm2
  restarted, watchdog armed): TOTAL regex `/(?<!\d)[OU]\s?\d+/i` fixes the
  fused-marker drop + adds an F5 signal. Root cause + Jun 5 deploy-timeline
  correlation (`0da16bc`) in the PR body.

### Snapshot verification (live, post-deploy `/admin`)

- **`capperledger` ROI `-77.3% → -100%`** — the only value that moved across all
  24 cappers (lost all risked capital; the old floor had understated it).
- **Top-3 now differentiated**, and **`dangambleai +2498.5%` is arithmetically
  real** — 49.97u profit on 2u risked via a `+5097` hit, **not** a cap artifact.
  There is **no live 500% cap**; the historical `+500%` cluster was the old
  export's clamp, removed pre-`faa88208`. (Closes the long-standing "Capper ROI
  display bug" + "2498.5% after Scoot override" backlog items.)
- **Brave shows the honest `healthy (9m ago)` state** — the #74 last-success
  age rendering working as intended.

### LockedIn handle swap (end-to-end)

`TeamLockTalk` retired, `lockedin_sportz` wired into all three sources of truth:
box-local DubClub `config.json` (`TeamLockTalk` removed, backup
`config.json.bak-20260610`, boot → `"1 capper(s): GuessAndPrayBets"`); Fly
`scraper_handles` (`enabled`, dated note; scraper → `"[Handles] fetched 8 active
from Fly"`); Fly `tracked_twitter` (`display_name='LockedIn'`, channel
`1485091165308190780`). **Architecture finding:** the handle source of truth is
`scraper_handles` on Fly served at `HANDLES_URL` (`x-mobile-secret` auth); the
scraper's `active_handles.json` is a write-through cache with built-in fallback.
Capper attribution derives from `tracked_twitter.display_name`, and a handle
**without** a `tracked_twitter` row attributes under the **raw handle** — the root
cause of the `LockedIn`/`lockedin_sportz` and `guess_pray_bets` duplicate-capper
splits (now a BACKLOG "Capper dedup / merge" item).

### S1a — first search-arc classification (read-only probe, pool = 302)

Tennis **68** (50 parlay / 18 straight, no source), MLB **65** (61 parlay), NBA
**57** (43 parlay + 2 quarantined missing-legs), NHL **29** (22 parlay) — so
~**151** (≈half the pool) are **prop-heavy parlays inside adapter-covered
sports**: the real gap is props-within-covered-sports, not just uncovered sports.
Soccer family fragmented across **5 labels** (Soccer 31 / Serie A 4 / EPL 2 /
SOCCER 1 / UCL 1 = 39); combat 35 (UFC 27 / MMA 5 / Boxing 3); ~80% of the pool
is >14 days old (favors a BDL historical backfill). **Preliminary S3
arbitration:** BDL NBA props first, MLB Stats API second, tennis adapter third.
**S1b** re-measures after ~1 week of honest attempts and adds a `parlay_legs`
prop-keyword cut to size the props-in-covered-sports slice directly.

### Process note — worktree-absolute paths (→ prompt-template rule)

Earlier the same day, **two agents accidentally edited the shared `main`
checkout** instead of their worktree because their prompts didn't pin paths. This
batch-2 prompt mitigated it by mandating **worktree-absolute paths for every file
operation**, and the run stayed cleanly isolated on its own branch. **Adopt as a
standing prompt-template rule:** doc/code prompts that run in a worktree must
instruct the agent to use worktree-absolute paths for all reads/writes and to
`git branch --show-current` before any commit (see
`feedback_shared_checkout_concurrency` memory).
