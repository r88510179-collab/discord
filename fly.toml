app = 'bettracker-discord-bot'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  NODE_ENV = 'production'
  PORT = '8080'
  RESOLVER_URL = 'http://zonetracker-resolver.internal:8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[mounts]
  source = 'bettracker_data'
  destination = '/data'

# 512MB needed for Vision AI image buffers + Discord.js cache + SQLite
[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
